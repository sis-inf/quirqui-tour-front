---
import Layout from '../layouts/Layout.astro';
import PiePagina from '../components/piePagina.astro';
import Encabezado from '../components/encabezado.astro';
---

<Layout title="Información Turística">
  <Encabezado />
  <main>
    <section class="infturistica-detalles">
      <div class="header">
        <h1>NOMBRE</h1>
      </div>

      <div class="content">
        <div class="column">          
          <!-- Filtro para cambiar a otros sitios turísticos -->
          <div class="tour-filter">
            <select id="tour-selector">
              <option value="">Ver otro sitio ...</option>
              <!-- Aquí puedes añadir más opciones -->
            </select>
          </div>
          <br>
          <!-- Imagen Y precio del sitio turístico -->
          <img src="/diablada.jpg" alt="Título del Sitio Turístico" />
          <div class="precio">Costo: </div>
        </div>

        <div class="info">
          <div class="titulo">HISTORIA</div>
          <div class="contenido">
            <p>Descripción detallada del sitio turístico. Aquí se proporciona información relevante sobre el destino.</p>
          </div>
        </div>
        
        <div class="column">
          <h3 class="horario">Horario de Atención</h3>
          <div class="hours">
            <p>8:00 AM - 6:00 PM</p>
          </div>

          <!-- Ubicación en Google Maps -->
          <div class="google-map">
            <h3>Ubicación</h3>
            <iframe 
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d7589.985734378003!2d-67.12093838352422!3d-17.979068842262727!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x93e2b0eb67b092bd%3A0xa40c21e575489880!2sZool%C3%B3gico%20Andino!5e0!3m2!1ses!2sbo!4v1733367898365!5m2!1ses!2sbo" 
              width="100%" 
              height="250" 
              style="border:2px solid rgb(115, 190, 39); border-radius: 8px;" 
              allowfullscreen="" 
              loading="lazy"
            ></iframe>
          </div>
          
          <!-- Espacio para un anuncio -->
          <div class="ad-space">
            <p>¡No te pierdas nuestras promociones y ofertas especiales!</p>
          </div>
        </div>
      </div>

      <!-- Botón para atrás -->
      <div class="back-button">
        <a href="javascript:history.back()" class="back-button-link">Volver a los destinos</a>
      </div>
    </section>
  </main>
  <PiePagina />
</Layout>
<style>
  :root {
        --cafe: #8B4512;
        --beige: #F5ECDD;
        --verde-musgo: #566B30;
        --rojo-vivo: #A52B2A;
        --azul: #335669;
      }

  .infturistica-detalles {
    padding: 20px;
  }

  .header {
    margin-bottom: 20px;
  }

  .header h1 {
    color: var(--rojo-vivo);
    font-size: 2em;
  }

  .content {
    display: flex;
    gap: 20px;
    justify-content: space-between;
  }

  .column {
    flex: 1;
  }

  .column img {
    width: 84%;
    max-width: 500px;
    margin-bottom: 20px;
    border: 5px solid #ccc; 
    border-radius: 10px; 
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); 
    transition: 0.3s ease; 
  }

  .column img:hover {
      transform: scale(1.05); 
  }

  .precio {
		background-color: var(--azul);
		color: var(--beige);
		padding: 8px;
		border-radius: 5px;
		text-align: center;
    justify-self: center;
    width: 83%;
		font-size: 1.5em;
	}
  
  .tour-filter {
    margin-top: 20px;
  }

  .tour-filter select {
    background-color: var(--rojo-vivo);
    color: var(--beige);
    padding: 10px;
    width: 85%;
    font-size: 16px;
    border-radius: 5px;
  }
  .info {
    width: 30%;
    display: flex;
    align-items: flex-start;
    border: 1px solid #233850;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    padding: 20px;
    background-color: var(--azul);
    margin-top: 20px;
    margin-right: 30px;
  }

  .titulo {
    background-color: var(--rojo-vivo);
    color: var(--beige);
    padding: 10px;
    border-radius: 5px;
    margin-right: 20px;
    font-size: 1.2em;
    font-weight: bold;
    white-space: nowrap;
  }

  .contenido {
    flex: 1;
    color: var(--beige);
  }
  .horario{
    color: var(--azul);
  }
  .hours {
    border: 2px solid #700909; 
    border-radius: 10px; 
    padding: 20px; 
    background-color: var(--rojo-vivo); 
    margin-top: 20px; 
    color: var(--beige);
    margin-right: 30px;
  }
  .google-map{
    margin-right: 30px;
  }
  .google-map h3 {
    margin-top: 40px;
    margin-bottom: 10px;
    color: var(--azul);
  }

  .ad-space {
    margin-top: 20px;
    text-align: center;
    padding: 20px;
    background-color: #f0f0f0;
    border-radius: 8px;
  }

  .ad-space p {
    font-size: 1.2em;
    color: #566B30;
  }

  /* Estilos del botón de regreso */
  .back-button {
    margin-top: 30px;
    text-align: center;
  }

  .back-button-link {
    padding: 10px 20px;
    background-color: #cb6b27;
    color: var(--beige);
    text-decoration: none;
    border-radius: 5px;
    cursor: pointer;
  }
  .back-button-link:hover {
    transform: translateY(-2px); 
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
  }
  .back-button-link:active {
    transform: scale(0.95);
  }

/* Media Queries para Tablets */
@media (max-width: 1204px) {
    .column img, .precio {
        width: 97%;
    }
    .tour-filter select{
      width: 100%;
    }
    .info {
        width: 34%;
        margin-right: 0;
    }

    .hours, .google-map {
        margin-right: 0;
    }
}

/* Media Queries para Móviles */
@media (max-width: 768px) {
    .header h1 {
        font-size: 1.5em;
    }

    .content {
        flex-direction: column;
        align-items: center;
    }
    .column img, .precio {
        width: 94%;
    }
    .tour-filter select{
      width: 98%;
    }
    .info {
        width: 93%;
        margin-right: 0;
    }

    .hours, .google-map {
        margin-right: 0;
    }

    .google-map iframe {
        width: 100%;
    }
}
 </style>